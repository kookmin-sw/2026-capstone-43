# 02_pipeline

## GitHub Cleanup Notes

This GitHub-ready copy was renumbered from `03_pipeline` to `02_pipeline`.
Generated experiment artifacts, logs, Python caches, and model weights were removed from this copy. The useful result metadata is summarized below so the repository stays lightweight.

### Removed Artifacts
- `__pycache__/`: 26 files, 114.9 KB
- `outputs/`: 62 files, 4.2 MB

### Result Summary
- Audio-only AmbiX batch: requested=10, selected=10, processed=10, failures=0, runtime=12.199 sec, mean intensity azimuth error=-3.00 deg, mean beam azimuth error=1.78 deg.
- Config snapshot: channel_order=WYZX, target_sr=24000, duration=8.0 sec, azimuth_bins=181, elevation_bins=91.
